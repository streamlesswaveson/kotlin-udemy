package udemy.l3_19_miscfeatures


fun main(args: Array<String>) {

    val list = arrayListOf("aidan", "iain", "andrew")

    // use brackets/index to access elements from array list
    println(list[0])


    //strings - kotlin has its own string class

    //exceptions - no distinction between checked/unchecked exceptions

    // ternery operator - there is none

    // no for loop i.e. for (int i=0; i<10; i++) {...

    // no 'new' keyword
}